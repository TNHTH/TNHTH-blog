{
  "name": "tnhth-blog",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@10.33.2",
  "engines": {
    "node": ">=24 <25",
    "pnpm": ">=10 <11"
  },
  "scripts": {
    "dev": "astro dev",
    "build:astro": "astro build",
    "search:index": "pagefind --site dist",
    "build": "pnpm build:astro && pnpm search:index",
    "start": "node scripts/serve-dist.mjs",
    "preview": "astro preview",
    "check": "astro check",
    "test": "vitest run",
    "publisher": "tsx scripts/publisher.ts",
    "content:verify": "tsx scripts/validate-public.ts --snapshot",
    "audit:public": "tsx scripts/validate-public.ts --snapshot",
    "sync:github": "tsx scripts/sync-github.ts",
    "hooks:install": "node scripts/install-git-hooks.mjs",
    "verify": "pnpm content:verify && pnpm audit:public && pnpm check && pnpm test",
    "test:e2e": "playwright test",
    "ci": "pnpm verify && pnpm build && pnpm test:e2e",
    "build:sites": "pnpm build && node scripts/prepare-sites-runtime.mjs",
    "serve:dist": "node scripts/serve-dist.mjs"
  },
  "dependencies": {
    "@astrojs/sitemap": "^3.6.0",
    "astro": "^6.0.0",
    "dompurify": "^3.4.13",
    "marked": "^18.0.9",
    "tailwindcss": "^4.1.0",
    "yaml": "^2.8.0"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.4",
    "@tailwindcss/vite": "^4.1.0",
    "@playwright/test": "^1.55.0",
    "pagefind": "^1.5.2",
    "sharp": "^0.34.5",
    "tsx": "^4.19.0",
    "typescript": "^5.8.0",
    "vitest": "^3.2.0"
  }
}
