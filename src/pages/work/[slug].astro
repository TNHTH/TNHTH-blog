---
export async function getStaticPaths() {
  return ["dashgo-rl-navigation", "dual-arm-robotics", "leap-a20"].map((slug) => ({ params: { slug } }));
}
return Astro.redirect(`/projects/${Astro.params.slug}`);
---
