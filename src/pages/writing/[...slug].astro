---
export async function getStaticPaths() {
  return [{ params: { slug: "spec-driven-development" } }];
}
import LegacyRedirect from "../../components/LegacyRedirect.astro";
const target = `/notes/${Astro.params.slug}`;
---
<LegacyRedirect source={`/writing/${Astro.params.slug}`} target={target} />
